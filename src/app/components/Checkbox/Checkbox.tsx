import React from 'react';
import styles from './Checkbox.module.css';

const Checkbox = () => {
    return (
        <input type="checkbox" className={styles.checkbox} id="html" />
    );
};

export default Checkbox;